apiVersion: v1
kind: Service
metadata:
  name: {{ include "harness-gitops-agent-helm.fullname" . }}
  labels:
    {{- include "harness-gitops-agent-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "harness-gitops-agent-helm.selectorLabels" . | nindent 4 }}
